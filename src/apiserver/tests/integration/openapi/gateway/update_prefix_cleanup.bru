meta {
  name: update_gateway_prefix_cleanup
  type: http
  seq: 25
}

delete {
  url: {{scheme}}://{{host}}/api/v1/open/gateways/test-update-conflict-gateway/
}

headers {
  Content-Type: application/json
  X-BK-API-TOKEN: {{x_bk_api_token}}
}

tests {
  test("清理更新测试网关", function() {
    // 清理测试创建的网关，允许 204（成功删除）
    expect([204]).to.include(res.status);
  });
}
