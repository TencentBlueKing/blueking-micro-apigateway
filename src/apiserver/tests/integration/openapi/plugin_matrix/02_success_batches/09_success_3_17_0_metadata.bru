meta {
  name: plugin_matrix_success_3_17_0_metadata
  type: http
  seq: 9
}

post {
  url: {{scheme}}://{{host}}/api/v1/open/gateways/bk-apisix-plugin-matrix-317/resources/plugin_metadatas/
}

headers {
  Content-Type: application/json
  X-BK-API-TOKEN: {{x_bk_api_token}}
}

body {
  [
    {
      "config": {
        "id": "batch-requests",
        "max_pipeline_items": 100
      },
      "name": "batch-requests"
    },
    {
      "config": {
        "id": "kafka-logger",
        "max_pending_entries": 1000
      },
      "name": "kafka-logger"
    }
  ]
}

assert {
  res.status: eq 200
  res.body.data.length: eq 2
}
