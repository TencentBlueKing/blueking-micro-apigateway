meta {
  name: plugin_matrix_invalid_3_11_0_tcp-logger_root-wrong-type
  type: http
  seq: 60
}

post {
  url: {{scheme}}://{{host}}/api/v1/open/gateways/bk-apisix-plugin-matrix-311/resources/routes/
}

headers {
  Content-Type: application/json
  X-BK-API-TOKEN: {{x_bk_api_token}}
}

body {
  [
    {
      "config": {
        "labels": {
          "env": "test"
        },
        "methods": [
          "GET"
        ],
        "plugins": {
          "tcp-logger": "invalid-object"
        },
        "upstream": {
          "nodes": [
            {
              "host": "httpbin.org",
              "port": 80,
              "weight": 1
            }
          ],
          "scheme": "http",
          "type": "roundrobin"
        },
        "uris": [
          "/plugin-tcp-logger"
        ]
      },
      "name": "plugin-tcp-logger"
    }
  ]
}

assert {
  res.status: eq 400
  res.body.error.code: eq "BadRequest"
}
