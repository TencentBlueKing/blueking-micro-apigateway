meta {
  name: consumer_group_batch_publish
  type: http
  seq: 20
}

post {
  url: {{scheme}}://{{host}}/api/v1/open/gateways/{{gateway_name}}/resources/consumer_groups/publish/
}

headers {
  X-BK-API-TOKEN: {{x_bk_api_token}}
  Content-Type: application/json
}

body {
  {
    "ids": ["{{group_id}}"]
  }
}

assert {
  res.status: eq 201
}