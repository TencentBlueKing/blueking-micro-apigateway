meta {
  name: plugin_metadata_batch_publish
  type: http
  seq: 15
}

post {
  url: {{scheme}}://{{host}}/api/v1/open/gateways/{{gateway_name}}/resources/plugin_metadatas/publish/
}

headers {
  X-BK-API-TOKEN: {{x_bk_api_token}}
  Content-Type: application/json
}

body {
  {
    "ids": ["{{plugin_metadata_id}}"]
  }
}

assert {
  res.status: eq 201
}