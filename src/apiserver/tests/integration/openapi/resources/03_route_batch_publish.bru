meta {
  name: route_batch_publish
  type: http
  seq: 17
}

post {
  url: {{scheme}}://{{host}}/api/v1/open/gateways/{{gateway_name}}/resources/routes/publish/
}

headers {
  X-BK-API-TOKEN: {{x_bk_api_token}}
  Content-Type: application/json
}

body {
  {
    "ids": ["{{route_id}}"]
  }
}

assert {
  res.status: eq 201
}