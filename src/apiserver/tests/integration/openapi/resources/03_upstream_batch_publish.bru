meta {
  name: upstream_batch_publish
  type: http
  seq: 21
}

post {
  url: {{scheme}}://{{host}}/api/v1/open/gateways/{{gateway_name}}/resources/upstreams/publish/
}

headers {
  X-BK-API-TOKEN: {{x_bk_api_token}}
  Content-Type: application/json
}

body {
  {
    "ids": ["{{upstream_id}}"]
  }
}

assert {
  res.status: eq 201
}