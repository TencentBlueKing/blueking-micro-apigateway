meta {
  name: consumers_group_update
  type: http
  seq: 23
}

put {
  url: {{scheme}}://{{host}}/api/v1/open/gateways/{{gateway_name}}/resources/consumer_groups/{{group_id}}/
}

headers {
  X-BK-API-TOKEN: {{x_bk_api_token}}
  Content-Type: application/json
}

body {
  {
      "name": "test_group-22",
      "config": {
          "desc": "",
          "labels": {
              "a": "1",
              "b": "2"
          },
          "name": "test_group-22",
          "plugins": {
              "jwt-auth": {
                  "algorithm": "HS256",
                  "key": "your_jwt_key"
              }
          }
      }
  }
}

assert {
  res.status: eq 204
}
