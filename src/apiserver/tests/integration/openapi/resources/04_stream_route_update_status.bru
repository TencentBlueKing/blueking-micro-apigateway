meta {
  name: get_stream_route_status
  type: http
  seq: 26
}

get {
  url: {{scheme}}://{{host}}/api/v1/open/gateways/{{gateway_name}}/resources/stream_routes/{{stream_route_id}}/status/
}

headers {
  Content-Type: application/json
  X-BK-API-TOKEN: {{x_bk_api_token}}
}

assert {
  res.status: eq 200
  res.body.data.status: eq update_draft
  res.body.data.id: matches /^bk.*/
}
