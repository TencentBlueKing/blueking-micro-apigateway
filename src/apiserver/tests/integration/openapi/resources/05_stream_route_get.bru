meta {
  name: get_stream_route
  type: http
  seq: 32
}

get {
  url: {{scheme}}://{{host}}/api/v1/open/gateways/{{gateway_name}}/resources/stream_routes/{{stream_route_id}}/
}

headers {
  Content-Type: application/json
  X-BK-API-TOKEN: {{x_bk_api_token}}
}

assert {
  res.status: eq 200
  res.body.data.name: eq test_stream_route-222
  res.body.data.id: matches /^bk.*/
}
