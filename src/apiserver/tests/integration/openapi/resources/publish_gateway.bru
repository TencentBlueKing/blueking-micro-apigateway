meta {
  name: publish_gateway
  type: http
  seq: 34
}

post {
  url: {{scheme}}://{{host}}/api/v1/open/gateways/{{gateway_name}}/publish/
}

headers {
  Content-Type: application/json
  X-BK-API-TOKEN: {{x_bk_api_token}}
}

assert {
  res.status: eq 201
}
